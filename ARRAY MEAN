#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);

    int arr[n];
    float sum = 0;

    for (int i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
        sum += arr[i];
    }

    float mean = sum / n;
    printf("Array Mean Value is %.2f\n", mean);

    return 0;
}
