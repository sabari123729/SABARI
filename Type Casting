#include <stdio.h>

int main() {
    int num;
    char ch;

    // Read inputs
    scanf("%d %c", &num, &ch);

    // Convert and print results
    printf("Integer of %d is %c\n", num, (char)num);
    printf("Character of %c is %d\n", ch, (int)ch);

    return 0;
}
