#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);

    float product = 1.0;
    for (int i = 0; i < n; i++) {
        float value;
        scanf("%f", &value);
        product *= value;
    }

    printf("The Product of list value is %.2f\n", product);

    return 0;
}
